/*
import { FunctionComponent } from "react";
import { toggleOnClick } from "./duck/actions";

export const withToggleOnClick = ({ componentId }: { componentId: string }) => (BaseComponent: FunctionComponent) => {
  const onClick = () => dispatch(toggleOnClick({ componentId }));
  return onClick;
};
*/
