import * as React from "react";
import { SFC } from "react";
import { Header } from "../Header";

type Props = {};

export const FlashcardPage: SFC<Props> = () => (
  <div>
    <Header />
    <div>Hello there.</div>
  </div>
);
