import * as React from "react";
import { FunctionComponent } from "react";
import { Header } from "../Header";

type Props = {};

export const FlashcardPage: FunctionComponent<Props> = () => (
  <div>
    <Header />
    <div>Hello there.</div>
  </div>
);
